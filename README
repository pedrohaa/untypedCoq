

This Coq code models and proves some interesting results concerning the lambda calculus. Also, it is implemented the definition and proofs of an abstract machine which executes lambda terms (Krivine Abstract Machine).

To compile it, just run the command:

make

And you're set to use this small module.
